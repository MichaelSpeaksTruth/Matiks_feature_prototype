# ─────────────────────────────────────────────────────────────────────────────
# Matiks Moderation Backend — Python Dependencies
# Minimal install (~50 MB) — no PyTorch, no CLIP, no local ML models.
# Vision inference is handled entirely by the Groq API.
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0

# ASGI server
uvicorn[standard]==0.30.1

# Multipart form / file upload support
python-multipart==0.0.9

# Groq Python SDK
groq==0.9.0
